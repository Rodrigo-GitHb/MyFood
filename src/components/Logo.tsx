import styled from 'styled-components'

export const Logo = styled.span`
  display: inline-block;
  color: var(--primary);
  font-size: 36px;
  font-weight: 900;
  letter-spacing: 0;
  line-height: 1;
`
